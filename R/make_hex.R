library(hexSticker)

imgurl <- "images/taxi.png"
sticker(
  imgurl, package = "taxastand",
  p_size = 20, s_x = 1, s_y = .75, s_width = .5, s_height = .5,
  filename = "images/taxastand_sticker.png")
